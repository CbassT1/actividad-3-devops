version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
  build:
    commands:
      - echo "Ejecutando pruebas"
      - pytest tests/
  post_build:
    commands:
      - echo "Construccion finalizada con exito"